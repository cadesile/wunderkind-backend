# Archived MySQL Migrations

29 migrations written against MySQL 8.0.
Archived during migration to PostgreSQL (2026-03-26).
Do not run these — they contain MySQL-specific syntax (ENGINE=InnoDB, BINARY(16) UUIDs, AUTO_INCREMENT).
A single fresh baseline migration replaces them for Postgres.
