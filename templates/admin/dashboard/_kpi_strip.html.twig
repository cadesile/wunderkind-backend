{% import 'admin/dashboard/_macros.html.twig' as dash %}

{{ dash.section('Platform', 'rolling windows · 30-day trend') }}

<div class="row g-3" data-kpi-strip data-src="{{ path('admin_stats_growth') }}">
    {{ dash.kpi('users',        'Users',         'fa-users',                stats.users) }}
    {{ dash.kpi('clubs',        'Clubs',         'fa-shield-halved',        stats.clubs) }}
    {{ dash.kpi('syncs',        'Syncs',         'fa-rotate',               stats.syncs) }}
    {{ dash.kpi('activeClubs',  'Active Clubs',  'fa-signal',               null) }}
    {{ dash.kpi('invalidSyncs', 'Invalid Syncs', 'fa-triangle-exclamation', stats.invalidSyncs, 'danger') }}
    {{ dash.kpi('poolPlayers',  'Pool Players',  'fa-person-running',       null, 'success') }}
    {{ dash.kpi('poolStaff',    'Pool Staff',    'fa-chalkboard-user',      null, 'success') }}
    {{ dash.kpi('poolAgents',   'Scouts / Agents', 'fa-binoculars',         null, 'success') }}
</div>
